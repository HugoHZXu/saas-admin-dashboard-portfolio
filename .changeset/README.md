# Changesets

This repo uses Changesets to version and publish the `hugo-ui` package.

Basic flow:

1. `npm run changeset` to add a changeset file.
2. `npm run changeset:version` to bump versions and update changelog.
3. `npm run changeset:publish` to publish.
