interface:
  display_name: "Activity Log UI Consumption"
  short_description: "Review dashboard consumption of backend-normalized Activity Log records."
  default_prompt: "Use $activity-log-normalization to review this Activity Log UI work against the external backend contract."
