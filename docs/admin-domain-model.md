# Admin Domain Model

The Admin domain model is backend-owned after the extraction to:

```text
/Users/xuhaoze/code-demo/hugo-saas-backend
```

Use the backend document for current Organization, User, Membership, Role, and Activity Log invariants:

```text
/Users/xuhaoze/code-demo/hugo-saas-backend/docs/admin-domain-model.md
```

This dashboard repository may document UI consumption patterns, but it should not redefine backend-owned role compatibility, seed data, database schema, or GraphQL contract rules.
